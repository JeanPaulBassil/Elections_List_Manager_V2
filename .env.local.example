# Public variables (used in browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Private variables (used for admin operations, never expose in browser)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# NOTE: The service role key has full admin rights to your database
# Only use it for server-side operations and NEVER expose it to the client
# You can find this key in the Supabase dashboard under Project Settings > API 